name: Pull Request Checks

on:
  pull_request:
    branches:
      - main
    paths:
      - "backend/**"
      - "frontend/**"
      - "sdk/**"
      - "python-sdk/**"
      - "package*.json"

jobs:
  test-and-coverage:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: |
            frontend/package-lock.json
            backend/package-lock.json
            sdk/package-lock.json

      - name: Install Node.js dependencies for frontend
        run: |
          npm ci --prefix frontend

      - name: Install Node.js dependencies for backend
        run: |
          npm ci --prefix backend

      - name: Install Node.js dependencies for sdk
        run: |
          npm ci --prefix sdk

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python SDK dependencies
        run: |
          python -m pip install --upgrade pip
          pip install httpx websockets pytest pytest-asyncio pytest-cov
        working-directory: python-sdk

      - name: Run frontend tests with coverage
        run: npm run test:coverage --prefix frontend

      - name: Check frontend coverage thresholds
        run: |
          cd frontend
          COVERAGE=$(node -e "
            const coverage = require('./coverage/coverage-summary.json');
            const total = coverage.total;
            const lines = total.lines.pct;
            const statements = total.statements.pct;
            const functions = total.functions.pct;
            const branches = total.branches.pct;
            console.log(JSON.stringify({ lines, statements, functions, branches }));
          ")
          echo "Frontend Coverage: $COVERAGE"

          LINES=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).lines)")
          STATEMENTS=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).statements)")
          FUNCTIONS=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).functions)")
          BRANCHES=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).branches)")

          THRESHOLD=50
          if (( $(echo "$LINES < $THRESHOLD" | bc -l) )); then
            echo "❌ Frontend line coverage ($LINES%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          if (( $(echo "$STATEMENTS < $THRESHOLD" | bc -l) )); then
            echo "❌ Frontend statement coverage ($STATEMENTS%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          if (( $(echo "$FUNCTIONS < $THRESHOLD" | bc -l) )); then
            echo "❌ Frontend function coverage ($FUNCTIONS%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          if (( $(echo "$BRANCHES < $THRESHOLD" | bc -l) )); then
            echo "❌ Frontend branch coverage ($BRANCHES%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          echo "✅ Frontend coverage meets all thresholds (${THRESHOLD}%)"

      - name: Run backend tests with coverage
        run: npm run test:coverage --prefix backend

      - name: Check backend coverage thresholds
        run: |
          cd backend
          COVERAGE=$(node -e "
            const coverage = require('./coverage/coverage-summary.json');
            const total = coverage.total;
            const lines = total.lines.pct;
            const statements = total.statements.pct;
            const functions = total.functions.pct;
            const branches = total.branches.pct;
            console.log(JSON.stringify({ lines, statements, functions, branches }));
          ")
          echo "Backend Coverage: $COVERAGE"

          LINES=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).lines)")
          STATEMENTS=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).statements)")
          FUNCTIONS=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).functions)")
          BRANCHES=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).branches)")

          THRESHOLD=50
          if (( $(echo "$LINES < $THRESHOLD" | bc -l) )); then
            echo "❌ Backend line coverage ($LINES%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          if (( $(echo "$STATEMENTS < $THRESHOLD" | bc -l) )); then
            echo "❌ Backend statement coverage ($STATEMENTS%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          if (( $(echo "$FUNCTIONS < $THRESHOLD" | bc -l) )); then
            echo "❌ Backend function coverage ($FUNCTIONS%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          if (( $(echo "$BRANCHES < $THRESHOLD" | bc -l) )); then
            echo "❌ Backend branch coverage ($BRANCHES%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          echo "✅ Backend coverage meets all thresholds (${THRESHOLD}%)"

      - name: Run SDK tests with coverage
        run: npm run test:coverage --prefix sdk

      - name: Check SDK coverage thresholds
        run: |
          cd sdk
          COVERAGE=$(node -e "
            const coverage = require('./coverage/coverage-summary.json');
            const total = coverage.total;
            const lines = total.lines.pct;
            const statements = total.statements.pct;
            const functions = total.functions.pct;
            const branches = total.branches.pct;
            console.log(JSON.stringify({ lines, statements, functions, branches }));
          ")
          echo "SDK Coverage: $COVERAGE"

          LINES=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).lines)")
          STATEMENTS=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).statements)")
          FUNCTIONS=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).functions)")
          BRANCHES=$(echo $COVERAGE | node -e "console.log(JSON.parse(require('fs').readFileSync(0, 'utf-8')).branches)")

          THRESHOLD=50
          if (( $(echo "$LINES < $THRESHOLD" | bc -l) )); then
            echo "❌ SDK line coverage ($LINES%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          if (( $(echo "$STATEMENTS < $THRESHOLD" | bc -l) )); then
            echo "❌ SDK statement coverage ($STATEMENTS%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          if (( $(echo "$FUNCTIONS < $THRESHOLD" | bc -l) )); then
            echo "❌ SDK function coverage ($FUNCTIONS%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          if (( $(echo "$BRANCHES < $THRESHOLD" | bc -l) )); then
            echo "❌ SDK branch coverage ($BRANCHES%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          echo "✅ SDK coverage meets all thresholds (${THRESHOLD}%)"

      - name: Run Python SDK tests with coverage
        run: python -m pytest tests/ --cov=finance_agent_sdk --cov-report=json --cov-report=term
        working-directory: python-sdk

      - name: Check Python SDK coverage threshold
        run: |
          cd python-sdk
          COVERAGE=$(python3 -c "
          import json
          with open('coverage.json') as f:
              data = json.load(f)
          print(data['totals']['percent_covered'])
          ")
          THRESHOLD=50
          if (( $(echo "$COVERAGE < $THRESHOLD" | bc -l) )); then
            echo "❌ Python SDK coverage ($COVERAGE%) is below threshold ($THRESHOLD%)"
            exit 1
          fi
          echo "✅ Python SDK coverage meets threshold (${THRESHOLD}%)"

      - name: Upload coverage reports
        uses: actions/upload-artifact@v4
        with:
          name: coverage-reports
          path: |
            frontend/coverage/
            backend/coverage/
            sdk/coverage/
            python-sdk/coverage.json
          retention-days: 7

      - name: Comment PR with coverage summary
        uses: actions/github-script@v7
        if: always()
        with:
          script: |
            const fs = require('fs');

            function getJsCoverage(path) {
              try {
                const summary = JSON.parse(fs.readFileSync(`${path}/coverage/coverage-summary.json`, 'utf8'));
                return summary.total;
              } catch (e) {
                return null;
              }
            }

            function getPythonCoverage(path) {
              try {
                const data = JSON.parse(fs.readFileSync(path, 'utf8'));
                return data.totals;
              } catch (e) {
                return null;
              }
            }

            const frontend = getJsCoverage('frontend');
            const backend = getJsCoverage('backend');
            const sdk = getJsCoverage('sdk');
            const pythonSdk = getPythonCoverage('python-sdk/coverage.json');

            const formatJsCoverage = (cov) => {
              if (!cov) return 'N/A';
              return `Lines: ${cov.lines.pct}% | Stmts: ${cov.statements.pct}% | Fns: ${cov.functions.pct}% | Branches: ${cov.branches.pct}%`;
            };

            const formatPythonCoverage = (cov) => {
              if (!cov) return 'N/A';
              const pct = cov.percent_covered.toFixed(1);
              return `Lines: ${pct}% (${cov.covered_lines}/${cov.num_statements} statements)`;
            };

            const threshold = 50;
            const jsCheck = (cov) => cov ? (cov.lines.pct >= threshold ? '✅' : '❌') : 'N/A';
            const pyCheck = (cov) => cov ? (cov.percent_covered >= threshold ? '✅' : '❌') : 'N/A';

            const warnings = [
              frontend && frontend.lines.pct < threshold ? '⚠️ Frontend coverage below threshold' : '',
              backend && backend.lines.pct < threshold ? '⚠️ Backend coverage below threshold' : '',
              sdk && sdk.lines.pct < threshold ? '⚠️ SDK coverage below threshold' : '',
              pythonSdk && pythonSdk.percent_covered < threshold ? '⚠️ Python SDK coverage below threshold' : '',
            ].filter(Boolean).join('\n');

            const comment = `## 📊 Code Coverage Report

            | Component | Coverage | Status |
            |-----------|----------|--------|
            | Frontend | ${formatJsCoverage(frontend)} | ${jsCheck(frontend)} |
            | Backend | ${formatJsCoverage(backend)} | ${jsCheck(backend)} |
            | SDK (npm) | ${formatJsCoverage(sdk)} | ${jsCheck(sdk)} |
            | Python SDK | ${formatPythonCoverage(pythonSdk)} | ${pyCheck(pythonSdk)} |

            **Minimum Required Coverage:** ${threshold}%
            ${warnings ? '\n' + warnings : ''}
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

  lint-and-format:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: |
            frontend/package-lock.json
            backend/package-lock.json
            sdk/package-lock.json

      - name: Install dependencies
        run: |
          npm ci --prefix frontend
          npm ci --prefix backend
          npm ci --prefix sdk

      - name: Run ESLint on frontend
        run: npm run lint --prefix frontend
        continue-on-error: true

      - name: Run ESLint on backend
        run: npm run lint --prefix backend
        continue-on-error: true

      - name: Run ESLint on SDK
        run: npm run lint --prefix sdk
        continue-on-error: true

  build:
    runs-on: ubuntu-latest
    needs: [test-and-coverage]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: |
            frontend/package-lock.json
            backend/package-lock.json
            sdk/package-lock.json

      - name: Install dependencies
        run: |
          npm ci --prefix frontend
          npm ci --prefix backend
          npm ci --prefix sdk

      - name: Build frontend
        run: npm run build --prefix frontend

      - name: Build backend
        run: npm run build --prefix backend

      - name: Build SDK
        run: npm run build --prefix sdk

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build Python SDK
        run: |
          pip install build
          python -m build
        working-directory: python-sdk
